diff --git chrome/common/controlled_frame/controlled_frame.cc chrome/common/controlled_frame/controlled_frame.cc
index b60aff5479745411e7feb17fcc7b81f377bec3f2..2b0941018ddaca1b6238a6d59b918c99c98c1b86 100644
--- chrome/common/controlled_frame/controlled_frame.cc
+++ chrome/common/controlled_frame/controlled_frame.cc
@@ -98,11 +98,13 @@ bool AvailabilityCheck(const std::string& api_full_name,
 
 #if BUILDFLAG(IS_WIN) || BUILDFLAG(IS_MAC) || BUILDFLAG(IS_LINUX) || \
     BUILDFLAG(IS_CHROMEOS)
-  // Verify that the current context is an Isolated Web App and the API name is
-  // in our expected list.
-  return (extension == nullptr) && url.SchemeIs(webapps::kIsolatedAppScheme) &&
+  // Verify that the current context has the Controlled Frame capability and
+  // the API name is in our expected list. The isolated-app scheme requirement
+  // is dropped; HasControlledFrameCapability() still enforces Permissions
+  // Policy, the application isolation level and the content setting.
+  return (extension == nullptr) &&
          context == extensions::mojom::ContextType::kWebPage &&
          context_data.HasControlledFrameCapability() &&
          std::ranges::contains(GetControlledFrameFeatureList(), api_full_name);
